# Admin shell

M1 provides only a real Next.js operational shell. It deliberately contains no
authentication, API client, business data or mutation capability.

Start it with:

```text
pnpm --filter @vse-pro-zhar/admin dev
```
