{
  "name": "vse-pro-zhar-mobile",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "24.14.1",
    "pnpm": "11.7.0"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pnpm lint:root && node scripts/checks/workspace-run.mjs --command lint",
    "lint:root": "eslint .",
    "typecheck": "pnpm typecheck:root && node scripts/checks/workspace-run.mjs --command typecheck",
    "typecheck:root": "tsc --project tsconfig.json --noEmit",
    "test:unit": "pnpm test:unit:root && node scripts/checks/workspace-run.mjs --command test:unit",
    "test:unit:root": "jest --config jest.config.cjs --runInBand",
    "check:test-hygiene": "node scripts/checks/test-hygiene.mjs",
    "test:architecture": "node scripts/checks/architecture.mjs",
    "check:automation-sync": "node scripts/checks/automation-sync.mjs",
    "check:checker-exit-codes": "node scripts/checks/checker-exit-codes.mjs",
    "check:task-contract": "node scripts/checks/task-contract.mjs",
    "check:task-scope": "node scripts/checks/task-scope.mjs",
    "check:diff-size": "node scripts/checks/diff-size.mjs",
    "check:secrets": "node scripts/checks/secrets.mjs",
    "check:dependencies": "node scripts/checks/dependencies.mjs",
    "verify:pr": "node scripts/checks/verify-pr.mjs",
    "build": "pnpm build:root && node scripts/checks/workspace-run.mjs --command build",
    "build:root": "tsc --project tsconfig.build.json",
    "verify:fast": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test:unit",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm check:test-hygiene && pnpm test:unit && pnpm test:integration && pnpm test:architecture && pnpm check:automation-sync && pnpm check:checker-exit-codes && pnpm build",
    "test:integration": "pnpm --dir apps/api test:integration"
  },
  "devDependencies": {
    "@babel/parser": "8.0.4",
    "@eslint/js": "10.0.1",
    "@jest/environment": "30.4.1",
    "@types/jest": "30.0.0",
    "@types/node": "24.13.3",
    "ajv": "8.20.0",
    "dependency-cruiser": "18.2.0",
    "eslint": "10.8.1",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-react-hooks": "7.0.1",
    "jest": "30.4.2",
    "jest-environment-node": "30.4.1",
    "jest-mock": "30.4.1",
    "prettier": "3.9.6",
    "ts-jest": "29.4.12",
    "typescript": "5.9.3",
    "typescript-eslint": "8.67.0",
    "yaml": "2.9.0"
  }
}
